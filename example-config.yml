# Example .claude/orchestrate.yml for a generic web project
# Copy this to your project's .claude/orchestrate.yml and customize

project:
  name: "My Project"
  description: "What your project does in one sentence"

# Base branch — where spoke work gets merged into
# Also the branch new work branches are created from
branches:
  base: "main"  # or "master", "develop", "demo", etc.

# Spoke types — enable/disable what you need
spokes:
  qa:
    enabled: true
    description: "Find bugs and edge cases"
    docs: "tests/"

  journey:
    enabled: false
    description: "Prospect validation (enable when you have personas)"
    personas: "docs/personas/"

  feature:
    enabled: true
    description: "Implement new features with TDD plans"
    plans: "docs/plans/"

  dev:
    enabled: true
    description: "Quick fixes and improvements"

  deploy:
    enabled: false
    description: "Setup hosting, configure deploy pipeline, debug production issues"

# Product context — teaches spokes about your app
# Fill this in for better QA and Journey sessions
product:
  modules:
    - name: "Dashboard"
      description: "Main view with analytics and KPIs"
      url: "/dashboard"
      roles: ["admin", "user"]
    # Add more modules as your app grows

  test_accounts:
    - role: "admin"
      login: "/auth/demo-login?role=admin"
      name: "Demo Admin"
      description: "Full access"
    - role: "user"
      login: "/auth/demo-login?role=user"
      name: "Demo User"
      description: "Regular user access"

  demo_data:
    description: "How your demo data was generated"
    scale: "10 items, 5 users"

# Deploy commands (all optional — fill in what applies)
deploy:
  build: "npm run build"
  frontend: ""
  backend: ""
  restart: ""

# Hosting credentials file (gitignored, NOT the credentials themselves)
hosting:
  credentials: ""  # e.g., "data/deploy_ssh.txt" or "data/cpanel_credentials.txt"
  type: ""         # "ssh", "cpanel", "vercel", "netlify", "railway", "fly", "github-actions"

# Verification commands (spokes run these on startup)
verify:
  - "git branch --show-current"
  - "git log --oneline -3"

# Project-specific warnings (included in spoke instruction blocks)
gotchas: []

# Roles in your app (used for QA and Journey context)
roles: []
